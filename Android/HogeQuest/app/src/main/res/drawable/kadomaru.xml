<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle" >

    <!-- 背景色 -->
    <solid android:color="#ffffff" />

    <!-- 角丸 (10dpで設定) -->
    <corners android:radius="10sp" />

    <!-- 枠線 widthは線の幅、colorは色 -->
    <stroke
        android:width="4sp"
        android:color="#ffffff" />
</shape>